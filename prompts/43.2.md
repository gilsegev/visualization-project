Goal: Restore the high-end "Wellness Book" aesthetic and fix the hub_radial alignment issues.

1. Hub Radial Alignment Fix (CSS/JSDOM):

    Open public/assets/infographics/templates/hub_radial.html.

    The Origin Fix: Ensure the central #hub-center and the .spoke-container share a common absolute parent with display: block (not flex).

    The Math Check: Verify the CSS variables --radius and --i. If the spokes are "drifting," it's because the transform: rotate() is being applied to a container that has its own internal padding. Remove all default padding/margin from spoke containers.

2. Content Enrichment (The "Wellness" Soul):

    Update the architectPrePass in CourseOrchestratorService.ts.

    Style Restoration: Move away from "Sticker-Style." The global_style_anchor for the Mindfulness course must now be:
    "Sophisticated wellness book illustration, soft watercolor bleeds, muted teal and sand tones, elegant charcoal line-art, high-end organic textures, serene and professional."

3. Multi-Layer Blending (Template Fix):

    Remove mix-blend-mode: multiply; from the images—it was making them look "dirty" on some backgrounds.

    Instead, use mask-image or a simple border-radius: 50% with a subtle inner shadow to make the AI icons feel "set into" the cards.

4. The "Side-by-Side" Validation:

    Re-run the "Autonomic Nervous System" (Hub) and "6 Stages of Stress" (Zig-Zag).

    The Test: Compare these new renders to the "Good" versions from yesterday.

    Success Criteria:

        The Hub center must be pixel-perfectly centered between all spokes.

        The images must have the "watercolor" texture back, not just flat teal icons.

        The text cards must be perfectly aligned with the icons they describe.

How this will be validated:

    Attach the new PNGs. They should feel "warm and expensive" again.