The "Enhanced Ascent" (Glass & Spine) template replaces step_stone.html.

This update fixes the readability issues you noticed by introducing staggered columns, a central vertical anchor, and glassmorphism text cards to ensure the text pops regardless of the background.

Here is Prompt 34 to fix the zig-zag and implement the final "Versus" template.
Prompt 34: Zig-Zag Overhaul & "Versus" Implementation

Goal: Replace the underperforming step_stone.html with the high-fidelity "Glass & Spine" version and implement the new "Versus" comparison template.

1. Update step_stone.html (The Zig-Zag Fix):

    Replace the current content of public/assets/infographics/templates/step_stone.html with the "Glass & Spine" layout logic:

        Structure: Use two 45% width columns with a 10% central gutter.

        Visuals: Add a vertical spine (line) through the center using a gradient ::before pseudo-element. Place a circular "Step Node" (01, 02, etc.) directly on the spine for each row.

        Readability: Apply backdrop-filter: blur(12px) and a semi-transparent white background to the text containers to ensure legibility.

        Alternating Logic: Ensure the .step-row:nth-child(even) CSS rule correctly flips the image and text positions.

2. Create versus_split.html (The Comparison):

    Create a new template at public/assets/infographics/templates/versus_split.html.

    Design: Use a vertical 50/50 split.

        Left Side: Subject A (e.g., Blue theme).

        Right Side: Subject B (e.g., Red theme).

        Center: A floating "VS" badge and a central spine for category labels.

    Injection Hooks: Use IDs #slot_img_a, #slot_img_b, #title_a, #title_b, and #metric_labels.

3. Strategy & Blueprint Update:

    In HtmlInfographicStrategy.ts, update generateBlueprint to support Comparison Logic:

        Selection: Choose versus_split when the prompt involves "Vs", "Compare", "Difference between", or two distinct subjects.

        Style Directive: For Versus, request "Studio portraits with cinematic rim lighting" to make the two subjects feel like a high-stakes face-off.

    Metric Injection: For the Versus template, have the agent map the comparison metrics generated by Gemini to the central spine labels.

4. End-to-End Validation:

    Test A (Zig-Zag Fix): Re-run the "iPhone Evolution" or "Career Path" test. Verify that the 10% gutter prevents text/image overlap and the glassmorphism makes the text sharp.

    Test B (Versus Launch): Run a test for "Electric Cars vs. Gasoline Cars."

        Verification: Ensure the final PNG shows a clear head-to-head split with a central "VS" badge.

How this will be validated:

    Visually compare the new career-journey.png (Zig-Zag) against the previous version; it should look significantly more professional and readable.

    Verify ev-vs-gas.png correctly utilizes the two-column split.