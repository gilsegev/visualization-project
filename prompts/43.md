Goal: Refactor all LLM calls to utilize OpenRouter (Gemini Flash 2.5) via an OpenAI-compatible client to solve 429 rate-limiting issues.

1. Dependency Update:

    Install openai package.

    Refactor Strategy: You will no longer use @google/generative-ai. Instead, use the standard OpenAI client.

2. Environment Configuration:

    Update all references to GEMINI_API_KEY to use OPENROUTER_API_KEY.

    Set the baseURL for the client to https://openrouter.ai/api/v1.

    Set the model identifier to google/gemini-flash-1.5-8b (or google/gemini-2.0-flash-001 based on OpenRouter availability).

3. Refactor CourseOrchestratorService.ts and HtmlInfographicStrategy.ts:

    Replace the model.generateContent calls with openai.chat.completions.create.

    Response Parsing: Ensure you are extracting the JSON from choices[0].message.content.

    System Prompts: Pass the "Expert Data Visualization Architect" and "Course Architect" instructions as system role messages.

4. Reliability & Backoff:

    Maintain the existing generateWithBackoff logic but adapt it for the OpenAI client.

    Add HTTP-Referer and X-Title headers to the OpenAI client configuration as required by OpenRouter.

5. Verification Test:

    Re-run scripts/test-full-course-run.ts.

    Validation: The logs should show the application connecting to OpenRouter. The batch should complete without 429 errors.

How this will be validated:

    The package.json should no longer contain @google/generative-ai.

    The .env file should have the OPENROUTER_API_KEY.