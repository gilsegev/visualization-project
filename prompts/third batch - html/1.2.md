🛠️ Prompt 1.1: The "Engine Preservation" Fix

Give this to the coding agent to stop the "stripping" and restore the rendering logic.

    Task: Stop Stripping the Render Engine from Hub.html

    1. Diagnose the "Slice" Bug:

        In TemplateStampingService.ts, check the stamp() method. It is currently returning an HTML string that is missing everything after the DATA_PAYLOAD.

    2. The "Full Blueprint" Requirement:

        You must treat src/templates/Hub.html as a single immutable string.

        Action: Use fs.readFileSync(templatePath, 'utf-8') to load the entire file into a variable.

        Action: Perform exactly one .replace() on the specific string /* INSERT_JSON_HERE */ null.

        Action: Do NOT try to manually rebuild the HTML or use JSDOM for this step. Simply replace the string and return the result.

    3. Deactivate PNG Output:

        Comment out await this.browserService.screenshot(...) in TemplateStampingStrategy.ts. We need to verify the HTML renders in a local browser first.

    4. Verification:

        Run scripts/test-stamping.ts.

        Success Criteria: >   - ✅ The generated HTML file must contain the <script> tag with the function render(data) logic.

            ✅ Opening the file in Chrome must show the 5 spokes.

            ✅ The file size should be significantly larger than the current "blank" output.