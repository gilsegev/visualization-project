Task: Implement TemplateStampingStrategy and Deprecate JSDOM Strategy

    Create New Strategy: Implement TemplateStampingStrategy.ts. This class should fulfill the InfographicStrategy interface but use the TemplateStampingService to generate the final HTML string.

    Implement Stamping: Instead of using JSDOM to manipulate the DOM, the strategy should:

        Load the Hub.html template as a string.

        Replace /* INSERT_JSON_HERE */ null with the stringified JSON data.

    Update Orchestrator: Update the factory logic to use TemplateStampingStrategy for the 'hub' template type.

    Deprecate Old Strategy: Rename the existing html-infographic.strategy.ts to DEPRECATED_jsdom-infographic.strategy.ts. Do not delete it yet.

    Validation: Ensure the app can still compile and that the new strategy successfully saves a "stamped" HTML file to the output/infographics/ directory.