    Task: Implement the "Visual Architect" System Prompt (Prompt 10)

    1. Persistent System Prompt Construction:

        Update the InfographicOrchestrator (or the relevant LLM service) to use a new, immutable System Prompt for all blueprint requests.

        Instruction to the Agent: You must ensure the LLM receives the following specific directives as its "Core Identity":

            Identity: "You are a Senior Visual Architect. You do not create content; you map pedagogical specifications into stable geometric blueprints."

            Hallucination Guardrail: "If the input data is sparse, return a correction_log instead of inventing items. You must preserve the EXACT terminology from the source."

    2. Template Catalog Integration:

        The System Prompt must include a technical catalog of your four verified templates: hub_radial, versus_split, step_journey, and bento_grid.

        Requirement: Define the exact JSON keys required for each (e.g., versus_split must have a subjects array with exactly 2 items).

    3. Implementation of the Quality Rubric:

        Instruct the LLM to calculate and return a quality_score (1-100) based on:

            Structural Fidelity (40 pts): Preservation of all branches/notes.

            Template Match (30 pts): Accuracy of the chosen geometry for the lesson goal.

            Wellness Alignment (30 pts): Adherence to the warm, non-clinical "Wellness Book" philosophy.

    4. Expected Output Schema:

        The orchestrator must expect a JSON response containing quality_score, template_id, and the template-specific blueprint object.

    5. E2E Validation Test (The "Two Pathways" Run):

        The agent must execute an E2E test using the following Input JSON as the User Prompt:

    JSON

    {
      "context": {
        "course_title": "Mindfulness & Stress Management",
        "design_philosophy": "Warm, calming wellness book aesthetic. Non-clinical.",
        "palette": ["#5B9A8B", "#F5E6D3", "#E8A598", "#4A5568", "#FAF9F6", "#1A365D"]
      },
      "visualization_spec": {
        "id": "1.1",
        "title": "The Stress Response: Two Pathways",
        "structure": {
          "top_node": { "label": "STRESSOR", "sub_node": "HYPOTHALAMUS" },
          "branches": [
            {
              "name": "SAM AXIS (Fast)",
              "sequence": ["SNS activates", "Adrenal Medulla releases Epinephrine"],
              "effects": ["Heart rate increases", "Blood pressure rises"]
            },
            {
              "name": "HPA AXIS (Slow)",
              "sequence": ["Hypothalamus releases CRH", "Pituitary releases ACTH"],
              "effects": ["Sustained energy", "Immune modulation"]
            }
          ],
          "footer_note": "Acute stress activates both. Chronic stress keeps them running."
        }
      }
    }

    3. Expected Output Verification:

        The agent must verify the LLM's response matches this Gold Standard Structure:

    JSON

    {
      "quality_score": [Number > 85],
      "template_id": "bento_grid", 
      "blueprint": {
        "background": { "color": "#F5E6D3" },
        "cells": [
          { "layout": { "col_span": 12, "row_span": 2 }, "content": { "type": "title_text", "title": "STRESSOR", "text": "HYPOTHALAMUS" } },
          { "layout": { "col_span": 6, "row_span": 8 }, "content": { "type": "title_text", "title": "SAM AXIS (Fast)", "text": "SNS activates... Heart rate increases" }, "style": { "border_color": "#E8A598" } },
          { "layout": { "col_span": 6, "row_span": 8 }, "content": { "type": "title_text", "title": "HPA AXIS (Slow)", "text": "Hypothalamus releases CRH... Sustained energy" }, "style": { "border_color": "#5B9A8B" } },
          { "layout": { "col_span": 12, "row_span": 2 }, "content": { "type": "text", "text": "Acute stress activates both. Chronic stress keeps them running." } }
        ]
      }
    }

    4. Logic Enforcement:

        Fail-Fast: If quality_score < 75, the agent must halt and log the correction_log.

        Text Integrity: Every specific term ("Hypothalamus", "CRH", "ACTH") must exist in the final blueprint.