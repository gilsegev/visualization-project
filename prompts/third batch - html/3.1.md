Phase 3.1: The "Asset Integrity" Patch

Give this prompt to the agent to fix the missing images before moving to Phase 4.

    Task: Fix Missing Spoke Images (Path Resolution & Render Wait)

    1. Fix Path Resolution:

        In TemplateStampingStrategy.ts, when calling browserService.screenshotHtml(html), you must provide a Base URL.
        * Action: Pass the absolute path of the public/generated-images/ directory as the baseUrl parameter to Playwright so it can resolve the ./assets/ relative paths.

    2. Enforce Asset Loading:

        In the strategy, add an explicit wait before the screenshot: await page.waitForSelector('img[src*="assets/spoke"]', { state: 'visible' }).
        * Add a "Safety Buffer" of await page.waitForTimeout(500) to ensure the GPU has finished painting the images into the circles.

    3. Image Generation Guardrails:
    * Ensure the generateImage logic logs a specific error if the SiliconFlow API returns a 404 or an empty buffer. Do not allow the stamping to proceed if an asset is missing.

    4. Validation:

        Re-run the 6-spoke scaling test.
        * Success Criteria: >   - ✅ The final PNG MUST show 6 unique images (no grey placeholders).
        - ✅ The console should log "All assets verified on disk" before the screenshot is taken.