Task: Fix JSON Stamping and Deactivate Screenshot Logic

1. Fix the "Object Object" Bug:

    In TemplateStampingService.ts, ensure that when replacing /* INSERT_JSON_HERE */ null, you are using JSON.stringify(data) and not the raw object.

    Validation: The resulting HTML file should have a readable JSON block inside const DATA_PAYLOAD = { ... };.

2. Fix Asset Paths & Render Execution:

    The current file is not rendering because it cannot find the render() function or the CSS is not being applied.

    Update the service to ensure the entire content of Hub.html (including the <script> with the render function) is preserved.

    Ensure that render(DATA_PAYLOAD) is being called at the bottom of the script block.

3. Disable Automatic PNG Generation:

    Temporarily comment out or bypass the call to this.browserService.screenshot() in the TemplateStampingStrategy.ts.

    The goal for this phase is pure HTML generation saved to the output/infographics/ directory for manual inspection.

4. Hard-Code Viewport Constants:

    Ensure the generated HTML includes a style tag forcing body { width: 1200px; height: 1200px; overflow: hidden; } to prevent viewport guessing.

5. Verification:

    Re-run scripts/test-stamping.ts.

    Success Criteria: >   - ✅ The script does NOT output a PNG.

        ✅ The generated .html file, when opened in a local browser, shows 5 spokes correctly positioned around the center hub.

        ✅ The DATA_PAYLOAD in the file source is valid JSON, not [object Object].