This is a massive win. The transition from a "flaky" JSDOM-based model to a deterministic, data-stamped HTML engine is now complete and verified. By restoring the generateImage logic and implementing local asset mapping, you have successfully bridged the gap between raw data and a high-fidelity visual asset.

As a senior TPM, you've now established a stable "Base Class" for your infographic pipeline. It's time to Stress Test the system to ensure the mathematical precision holds up as we scale.
Phase 3: The Scaling Stress Test

Goal: Prove that the "Magnet Anchor" math and image generation logic are robust enough to handle any content volume (3 to 7 spokes) without manual intervention.

Prompt for the Coding Agent:

    Task: Automated Scaling Stress Test (3–7 Spokes)

    1. Create a "Batch" Test Script:

        Implement a new script at scripts/test-hub-scaling-suite.ts.

        This script must iterate through counts i=3 to i=7.

    2. Dynamic Blueprint Generation:
    * For each iteration (i), generate a unique mindfulness-themed blueprint.

        Constraint: Each iteration must trigger the Full E2E Flow:
        - Generate i unique images.
        - Save images locally in the assets/ folder.
        - Stamp the Hub.html template with the correct relative paths.
        - Capture a final .png screenshot.

    3. Geometric Guardrails (Validation):
    * Ensure the script uses a radius of 360 in the CONFIG block to prevent edge-clipping at higher item counts.

        Implement a Verticality Check: In the generated PNGs, verify that "Item 1" is always perfectly centered at the 12 o'clock position (600,240).

    4. Success Criteria:

        ✅ Inventory Check: 5 unique HTML files and 5 unique PNG files exist in public/generated-images/.
        * ✅ Asset Audit: A total of 25 unique images (3+4+5+6+7) exist in the assets/ subdirectory.
        * ✅ Visual Quality: No text overlap or card clipping is present in the 7-spoke version.

    5. Final Report:

        The script should output a console table showing the generation time and success status for each count.