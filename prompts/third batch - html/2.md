Phase 2: The E2E Asset Pipeline Prompt

Since the "Stamping Engine" is now verified and correctly positioning the 5 spokes, it is time to enable the Full E2E Flow. This prompt will force the agent to generate the images, save them locally, and link them to your HTML.

    Task: Full E2E Integration (Asset Generation & Path Mapping)

    1. Activate Image Generation:

        Update TemplateStampingStrategy.ts to restore the parallel image generation logic from the deprecated strategy.

        For each item in the items array, generate a unique "wellness-style" image based on the item title and description.

    2. Implement Local Asset Storage:

        Create a folder at public/generated-images/assets/ if it does not exist.

        Save every generated spoke image locally as spoke_[timestamp]_[index].png.

    3. Map Local Paths to JSON:
    * In the DATA_PAYLOAD injected into the HTML, set item.image_url to the relative local path (e.g., ./assets/spoke_..._0.png).

        Crucial: Ensure the generated HTML file is saved in the same parent directory as the assets/ folder so the browser can resolve these paths.

    4. Re-enable Screenshots:

        Un-comment the this.browserService.screenshot() call in the strategy.

        Ensure Playwright waits for { waitUntil: 'networkidle' } to guarantee the local images are fully rendered before the snap.

    5. Validation (The "Proof of Life" Test):

        Run the e2e test.

        Success Criteria:

            ✅ Confirm public/generated-images/assets/ contains the new image files.

            ✅ Open the final .html file; it should now show unique generated images in the 5 circles.

            ✅ The final .png output must match the HTML exactly, with no missing images or alignment shifts.