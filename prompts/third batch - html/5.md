To utilize this template, the agent must generate a payload that supports both the center header and the background image.

    Task: Implement "Steps" Journey Orchestration

        Asset Generation: >    * Generate N images for the steps (3 to 5 items).

            Generate one large background image based on the overall theme (e.g., "Minimalist nature landscape for background, very soft focus").

        Payload Mapping:

            Background: Add background_url at the root of the JSON pointing to the local background image.

            Header: Map center_topic to center.title and center.subtitle.

            Items: Ensure each item includes a title, description, and the local image_url for its specific step.

        Validation: Generate a "4-Step Morning Routine" to verify that the horizontal connector line aligns with the cards and the background transparency is correct.