Task: Align Data Schema with HTML Render Engine

1. Fix the Property Mismatch:

    The Hub.html template expects data.center.title.

    The TemplateStampingService is currently providing a JSON with center_topic.

    Action: In the service, map the blueprint.center_topic to a new object key called center before stringifying it.

2. Implement Safe Stamping (Regex Fix):

    Use a callback replacer in your string replacement to prevent special characters in the description (like $) from breaking the replacement logic:

    .replace('/* INSERT_JSON_HERE */ null', () => JSON.stringify(data)).

3. Add "Null Guard" to Render:

    In the Hub.html template (or during injection), ensure the render call is wrapped in a try/catch block so we can see errors in the browser console if it fails again.

4. Validation:

    Run npx ts-node scripts/test-stamping.ts.

    Success Criteria: >   - ✅ Open the generated HTML.

        ✅ Press F12 to check the console. There should be 0 errors.

        ✅ The center hub should display "AI Agents" and the 5 spokes should be visible.