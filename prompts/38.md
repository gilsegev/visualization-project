Goal: Fix text overlapping and element collisions by enforcing a strict "Flow-Based" layout logic.

1. Template Refactoring (CSS Fix):

    Open all templates (hub_radial, step_stone, bento_grid, versus_split).

    The Box Model: Ensure no text container has a fixed height. Replace with height: auto and min-height: fit-content.

    Flex Layout: Wrap text and titles in a div with display: flex; flex-direction: column; gap: 0.5rem;. This ensures the title always pushes the description down rather than overlapping it.

2. JSDOM Injection Refinement:

    In HtmlInfographicStrategy.ts, update the cloning loop.

    Separation: Ensure that when a .spoke or .step-row is cloned, it is appended to the parent container in a way that allows CSS Flex/Grid to handle the spacing.

    Z-Index: Explicitly set position: relative and z-index: 10 on all text cards to ensure they never "slide under" the background lines.

3. Fail-Safe Text Truncation:

    Add this utility class to the global <style> block in all templates:
    CSS

    .line-clamp-4 {
      display: -webkit-box;
      -webkit-line-clamp: 4;
      -webkit-box-orient: vertical;  
      overflow: hidden;
    }

    Apply this class to all #slot_txt_n elements. This prevents the "Paragraph Bloat" that breaks the layout.

4. The "Stress Test" Validation:

    Create scripts/test-collision-fix.ts.

    Topic: "The 5 most complex laws of physics explained for a 5-year-old."

    Constraint: Force Gemini to write very long descriptions (50+ words).

    Validation: Check the PNG. The cards should expand vertically to accommodate the long text without overlapping the images or the cards below them.

How this will be validated:

    The final PNG should show all text fully contained within its "Glass" card, with clear gutters between every element.