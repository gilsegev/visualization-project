Goal: Implement responsive text scaling and enhanced "Glassmorphism" styling across the theme library.

1. Smart Text Scaling (JSDOM/CSS):

    Update all templates (hub_radial, step_stone, bento_grid) with a CSS "clamp" function for titles.

    Example: font-size: clamp(1.5rem, 4vw, 2.5rem);. This ensures titles scale down if the LLM generates a very long string.

2. The "Frost" Layer:

    In HtmlInfographicStrategy.ts, add a logic step that injects a backdrop-filter: blur(8px) and background: rgba(255, 255, 255, 0.1) (or dark equivalent) to all text containers.

    Why: This creates a "premium" layered look and guarantees the text is readable over any AI-generated icon.

3. Theme-Based Font Injection:

    Update the THEME_LIBRARY. Assign specific Google Fonts to each theme:

        cyber_neon: 'Orbitron' or 'JetBrains Mono'.

        nature_fresh: 'Quicksand'.

        corp_blue: 'Inter'.

        warm_creative: 'Playfair Display'.

    Ensure the strategy injects the correct @import link for these fonts.

4. High-Res Export Verification:

    Re-run the "Future of Quantum Computing" test.

    Validation: Check that the titles use the new 'Orbitron' (or equivalent) font and that the text cards have a soft, blurred "glass" look.

How this will be validated:

    Visually inspect the new PNG.

    Success Criteria: Does the infographic look like a static drawing, or does it feel like a modern, layered digital interface