Goal: Connect the BrowserService for PNG capture and replace the mock image generation with the real Silicon Flow API.

1. Real Asset Pipeline:

    Replace the generateImage mock in HtmlInfographicStrategy.ts with a real call to the Silicon Flow API.

    Refined Prompts: Update the logic to append blueprint.theme_accent colors or styles to the image generation prompt to ensure the icons share the same palette as the layout.

2. Browser Rendering (Playwright):

    In the performGeneration method, pass the final HTML string to the BrowserService.

    Capture Settings:

        Use deviceScaleFactor: 2.0 (for high-density "Retina" quality).

        Set the viewport to 1200x1200px.

        Focus the screenshot on the .hub-container or .list-container specifically to eliminate any unwanted white space.

3. The "All-Star" Final Validation:

    Create a final test script scripts/test-nba-allstar-final.ts.

    Topic: "The Top 5 NBA All-Stars (Jokic, SGA, Luka, Giannis, Wemby)."

    Validation:

        Verify nba-allstars.png is generated.

        Ensure the background is a professional dark gradient/texture.

        Check that the player names and 30-word bios are perfectly legible and correctly wrapped.

How this will be validated:

    Visually inspect the final PNG. It should look like a professional, high-end digital trading card hub.