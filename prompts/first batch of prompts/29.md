Goal: Complete the implementation of performGeneration in HtmlInfographicStrategy.ts by integrating parallel asset generation and DOM manipulation.

Instructions:

    Parallel Asset Generation:

        Use your existing SiliconFlow logic (or a mock service if not fully connected) to generate images for each item in the items array.

        Request a "background texture" image based on the theme_accent.

        Execute these requests in parallel using Promise.all.

    DOM Manipulation (JSDOM):

        Template Setup: Load the HTML via loadTemplate.

        Item Cloning: Identify the "Master Item" in the template (e.g., .spoke or .step). Clear the container and clone this master element N times.

        Data Injection: For each cloned element, inject:

            The Base64 image into the correct <img> tag.

            The Title and Description into their respective text tags.

            The CSS variables --i (index) and --total (total count) to trigger the CSS math.

    Theme Injection: Inject the theme_accent into the template's :root or a global <style> tag to update the glow and border colors.

How to validate: Generate the final HTML string for 5 items. Print the resulting HTML to the console (truncated) to verify that 5 distinct item blocks exist and contain data:image/png;base64 strings.