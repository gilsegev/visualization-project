Goal: Create the HtmlInfographicStrategy and the physical template directory.

    Structure: Create a directory public/assets/infographics/templates/.

    Seed Template: Save the provided "Hub" and "List" HTML templates into this directory as hub_radial.html and step_list.html.

    Strategy Implementation: Create HtmlInfographicStrategy.ts.

        It must include a loadTemplate(id: string) method that reads the file from the library.

        It must use jsdom to manipulate the template before rendering.

How to Validate: The agent should confirm it can read hub_radial.html and return its string content.