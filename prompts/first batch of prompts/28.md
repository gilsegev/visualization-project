Goal: Update the Gemini 2.0 Flash system prompt to act as a "SmartArt" selector.

    Selection Logic: Instruct the LLM that it must choose a template_id based on the user's intent:

        hub_radial: For lists of items centered around one topic (e.g., "Top 5 NBA Players").

        step_list: For sequential processes (e.g., "How to grow a tree").

    Blueprint Update: The output JSON must now include:

        template_id (string)

        theme_accent (hex code)

        items[]: Each item needs a title and a description (approx 30 words).

How to validate: Generate a blueprint for "The lifecycle of a star" and verify it selects step_list and provides descriptive paragraphs.