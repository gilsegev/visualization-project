Goal: Synchronize the "Road" path with the step images, implement multi-line text generation, and perform a final E2E multi-test.

1. Road & Image Alignment Fix:

    Update the snake_process.svg template (or the logic in renderComposition) so the road's path coordinates (x,y) match the translate centers of the step groups.

    The Curve: Use the following path for a 1400x600 viewBox to ensure the road flows between the circles:
    M -100 320 C 150 250, 350 250, 460 320 S 720 400, 850 320 S 1100 240, 1300 320

2. High-Fidelity Text Generation:

    Update the InfographicBlueprint interface and the Gemini 2.0 Flash system prompt.

    New Requirement: For each item, the LLM must generate:

        label_text: A punchy title (2-3 words).

        description_lines: An array of 3-4 short strings (max 40 chars each) to serve as the "Body Text."

    Injection: In renderComposition, inject these lines into the SVG using <tspan> elements within the #slot_txt_n group to ensure proper vertical stacking.

3. Multi-Scenario E2E Test:

    Scenario A (The Fun One): "A 5-step process on how a Squirrel plans a Winter Heist: 1. Recon, 2. Nut Selection, 3. Buried Treasure, 4. Defense, 5. Spring Feast."

    Scenario B (The Pro One): "The 5 stages of a Professional Coffee Taster's Morning: 1. Aroma, 2. Slurp, 3. Palette Map, 4. Body Note, 5. The Spitting."

    Execution: Both must generate 5 unique AI icons, 1 thematic background, and the multi-line text.

How this will be validated:

    Visual 1: Does the road look like the "path" the circles are sitting on?

    Visual 2: Is there a block of 3-4 lines of text clearly visible near each circle?

    Technical: Is the final output 1024x1024 with no white letterboxing?