Goal: Fix the "dirty square" effect and prompt pollution by implementing CSS blending and atomic prompt structures.

1. Template Blend Fix (CSS):

    Open all templates (hub_radial, step_stone, etc.).

    Apply mix-blend-mode: multiply; to all <img> tags that hold item icons.

    Why: This "burns" the white background away, ensuring the icon looks perfectly integrated into our cream-colored wellness cards.

2. Atomic Prompt Refactor (Strategy):

    In HtmlInfographicStrategy.ts, rewrite the generateImage prompt builder.

    STRICT Prompt Template: "{item_title}, minimalist line art, vector icon, flat colors, {theme_accent_color} accents, white background --no shadows, 3d, realistic, blurry, text."

    Remove all mentions of "8k," "professional," "studio lighting," or "volumetric."

3. The "Subject-Only" Rule:

    Instruct Gemini in the architectPrePass to focus the visual_style_directive on Medium and Color only (e.g., "Flat Vector, Pastel").

4. High-Resolution "Clean" Test:

    Re-run Visualization 1 (The Autonomic Nervous System).

    Validation:

        Is the "dirty square" gone? (The icon should look like it's printed directly on the card).

        Are the colors vibrant and the lines sharp?

How this will be validated:

    The final PNG should look like it was designed in Adobe Illustrator, not generated by an AI.