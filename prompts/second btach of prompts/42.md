Goal: Drastically improve image quality by moving to a "Sticker-Style" isolation logic and removing conflicting style directives.

    1. Refine the Style Anchor (Orchestrator):

        Update CourseOrchestratorService.ts. The global_style_anchor for the Mindfulness course should be simplified to:
        "Minimalist hand-drawn line art, flat colors, soft pastel aesthetic, high white-space, very clean, zen-like."

    2. Subject Isolation Logic (Strategy):

        In themes.config.ts (or the HtmlInfographicStrategy), update the generateImage prompt builder.

        The "Sticker" Rule: Force every item image to be "Isolated on a plain white background".

        Why: This allows our HTML/CSS background to "own" the page, while the icons look like professional, clean stickers floating in the slots.

        New Prompt Structure: [item.title], [visual_style_directive], flat vector icon style, isolated on white background, matching [primary_accent] color --no shadows, text, blurry, complex background.

    3. Background Texture Fix:

        For the background image, stop requesting "Abstract textures." Instead, request a "Very faint, high-quality paper texture, almost white".

    4. The "Clean Slate" Test:

        Re-run the Mindfulness batch.

        Success Criteria:

            Do the icons have clean edges?

            Is there zero "AI-generated text" inside the pictures?

            Does the text in the HTML cards feel separate and readable from the images?

    How this will be validated:

        The final PNG should look like a professional PDF or a high-end wellness app, not a "generated" collage.