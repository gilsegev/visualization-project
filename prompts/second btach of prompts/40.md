Goal: Refine the architectPrePass logic to ensure deep visual harmony across all 8 visualizations and intelligent template-to-topic mapping.

1. Intelligence Calibration (Gemini Prompting):

    Update the architectPrePass system prompt in CourseOrchestratorService.ts.

    New Rule: The global_style_anchor must be a high-density "Art Directive" (30-40 words). It should explicitly define:

        Lighting: (e.g., "Soft morning sunlight, gentle shadows").

        Texture: (e.g., "Grained paper texture, watercolor bleed").

        Composition: (e.g., "Centered subjects, minimalist whitespace, matching teal line art").

    Template Validation: Have the Architect verify the suggested_template for each viz. If a viz titled "The Stress Cycle" is suggested as a bento_grid, the Architect should override it to step_stone.

2. Theme Mapping Evolution:

    The theme_mapping should now support Contrast Safety. Ensure the Architect picks a 'text' color that is strictly legible against the 'background' color based on the provided palette.

3. Global Style Injection:

    Ensure the global_style_anchor is passed down as a mandatory prefix to every image generation prompt in the batch.

    Example Image Prompt: [global_style_anchor], [visualization.description], professional wellness illustration, high resolution.

4. High-Fidelity Batch Test:

    Re-run scripts/test-full-course-run.ts.

    Validation: Compare the first and last image generated in the Mindfulness course.

    Success Criteria:

        Both images must share the exact same lighting and artistic medium (e.g., both are watercolor or both are 3D).

        The teal accent color (#5B9A8B) must be used consistently for borders or highlights in both.

How this will be validated:

    The BatchResult should return the global_style_anchor. I will review this string to ensure it’s specific enough to hold the "vibe" together.