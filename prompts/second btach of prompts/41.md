Goal: Enhance image quality and visual consistency by mapping the exact course hex codes into the asset pipeline and refining the template CSS.

1. The "Mindfulness" Theme Injection:

    In HtmlInfographicStrategy.ts, create a temporary override or new theme called wellness_mindful using the specific hex codes:

        primary_accent: #5B9A8B (Muted Teal)

        background_main: #FAF9F6 (Cream White)

        text_main: #2D3748 (Deep Slate)

    CSS Variable Injection: Ensure these variables are injected into the :root of every template during the course batch run.

2. Technical Image Prompting:

    Update the generateImage method. It must now combine the global_style_anchor with Color Forcing.

    New Prompt Structure: [global_style_anchor], [item.description], primary color palette of [primary_accent] and [background_main], minimalist vector style, clean composition, soft lighting, 8k resolution, professional wellness illustration --no text, blurry, distorted, messy.

3. Template Readability Fix:

    Apply a max-width of 80% to all text containers and center them.

    For the step_stone (Zig-Zag) template, increase the gap between the image and text blocks by 20% to prevent the "clutter" feel.

4. The "Fidelity" Test:

    Re-run the first 3 visualizations of the Mindfulness course.

    Success Criteria:

        The teal (#5B9A8B) must be the dominant accent color in every icon.

        The background of the entire image should be the cream white (#FAF9F6) specified in the metadata.

        Text must not overlap any part of the player/subject icons.

How this will be validated:

    Visually inspect the generated PNGs. They should feel like they belong in the same high-end wellness book.