Goal: Implement Style Anchoring to ensure all generated icons and backgrounds share a consistent aesthetic.

1. Art Director (Gemini) Evolution:

    Update the HtmlInfographicBlueprint and the system prompt.

    New Field: visual_style_directive (e.g., "High-end 3D claymorphism, soft studio lighting, pastel color palette, minimalist").

    Instruction: Gemini must generate a single, cohesive style directive that applies to the entire set based on the topic.

2. The Asset Pipeline Refinement:

    In HtmlInfographicStrategy.ts, update the generateImage call.

    Prompt Construction: Every icon prompt must follow this structure: ${blueprint.visual_style_directive}, ${item.subject}, centered, high resolution, professional design, matching ${blueprint.theme_accent} color scheme.

    Background Sync: The background texture request should use the same style directive but with "abstract background" as the subject.

3. Image-to-Image (Optional/Advanced):

    If the API supports it, have the agent explore using the first generated image as a style_reference for the remaining four.

How this will be validated:

    Run the "Top 5 NBA All-Stars" test again.

    Success Criteria: Every player must look like they were drawn by the same artist (e.g., all 5 are 3D renders or all 5 are sleek black-and-white photos). No mixing of styles.