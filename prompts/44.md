Goal: Fix the Hub alignment, correct the center-data mapping, and restore artistic quality through Atomic Prompting.

1. Hub "Dead-Center" Fix (HTML/CSS):

    In hub_radial.html, place the #hub-center inside a position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); wrapper.

    Crucial: The center image and center text must have their own IDs: #slot_img_center, #slot_title_center, and #slot_txt_center.

2. The "Atomic" Prompt Engine (Strategy):

    Refactor generateImage in HtmlInfographicStrategy.ts.

    New Logic: If theme is wellness_mindful, force the prompt to: "${title}", hand-drawn charcoal lines, soft watercolor wash, ${accentColor} accents, minimal detail, white background --no 3d, text, shadows, frame.

    Background: The background image must be strictly: "high-quality heavy grain watercolor paper texture, cream-white #FAF9F6".

3. Data Mapping Update:

    Update performGeneration. For the hub_radial template:

        The First Item in the blueprint.items array must be mapped to the Center.

        All Subsequent Items (index 1+) must be mapped to the Spokes.

        This prevents the "Center Data is Wrong" issue by creating a strict 1-to-1 mapping.

4. High-Res Playwright Capture:

    Set viewport: { width: 1200, height: 1200 } and deviceScaleFactor: 3.

    Format: Use type: 'png' to preserve the sharp edges of the hand-drawn style.

5. The "Golden Standard" Test:

    Run the Mindfulness Hub test ("The Autonomic Nervous System").

    Success Criteria:

        The center title must say "Autonomic Nervous System."

        The spokes must radiate perfectly from that center.

        The images must look like watercolor art on real paper.