Goal: Standardize the Hub data contract and implement high-contrast "Solid" wellness styling to fix alignment and legibility.

1. Data Schema Refactor (Blueprint):

    Update HtmlInfographicStrategy.ts. The HtmlInfographicBlueprint interface must now have a mandatory center_topic property:
    TypeScript

    center_topic: { title: string; description: string; }
    items: { title: string; description: string; }[] // Now strictly only the spokes

    Update the generateBlueprint system prompt: "Place the core subject in center_topic. Place supporting details (3-8 items) in the items array. Do NOT repeat the center topic in the items array."

2. Hub Rendering Logic (The "Absolute" Fix):

    In performGeneration, explicitly map center_topic to #slot_title_center and #slot_txt_center.

    For the spokes, calculate the left and top coordinates in the loop:
    const angle = (index / total) * 2 * Math.PI;
    const x = 50 + Math.cos(angle) * radius_percent;
    const y = 50 + Math.sin(angle) * radius_percent;

    Inject these as inline styles: style="left: ${x}%; top: ${y}%;". This bypasses CSS "drift."

3. Wellness "High-Contrast" Polish:

    Create a THEME_MODIFIERS logic. If the theme is wellness_mindful:

        Disable Blur: Set backdrop-filter: none !important;.

        Solid Background: Set .glass-card to a solid, opaque #FFFFFF or #FAF9F6.

        Text Weight: Force all #text_main to font-weight: 600 for better readability.

4. Quality Verification Test:

    Re-run the "Autonomic Nervous System" Hub.

    Success Criteria:

        The center correctly displays the main title.

        There are exactly N spokes radiating with mathematical precision.

        The text is crisp and the "muddy" smear is gone.

How this will be validated:

    Provide the generated PNG and the console log showing the center_topic vs. items split.