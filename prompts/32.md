Goal: Enhance the HtmlInfographicStrategy to handle multiple template types and implement a "Style Archetype" library for visual consistency.

    1. Template Selection & Content Mapping:

        Update generateBlueprint to specifically choose step_stone for prompts that imply a timeline, journey, or sequence.

        Mapping Rule: For step_stone, ensure the agent maps the blueprint items to the #slot_num_n, #slot_img_n, and #slot_txt_n IDs.

    2. The Style Archetype Library:

        Create a new private method applyStyleArchetype(blueprint).

        If the prompt is "Tech/Corporate," apply the "Enterprise Blue" style (theme-accent: #2563eb).

        If the prompt is "Fun/Creative," apply the "Vibrant Sunset" style (theme-accent: #f59e0b).

        In the HTML injection, replace the CSS variable --theme-accent in the root style with the chosen hex code.

    3. Zig-Zag Animation (JSDOM):

        For the step_stone template, ensure that during the cloning process, the agent doesn't need to do extra work—the CSS :nth-child(even) rule already handles the zig-zag.

    4. The "Journey" E2E Test:

        Create scripts/test-career-journey.ts.

        Topic: "The 5 steps from a Junior Developer to a CTO."

        Requirement: The agent must use the step_stone template and the "Enterprise Blue" style.

    How this will be validated:

        Visually inspect career-journey.png.

        Verification: The image should alternate (Image Left -> Image Right -> Image Left). There should be a dashed/gradient line connecting the steps.