Goal: Implement a "Debug Mode" that exports raw HTML and logs internal coordinate math to diagnose hub misalignment and visual quality degradation.

1. Raw HTML Export (The "Source of Truth"):

    Update HtmlInfographicStrategy.ts. Before the Playwright screenshot, save the finalHtml string to a file: public/generated-images/courses/{course_id}/debug_{viz_id}.html.

    Why: This allows us to open the exact file in a browser to see if the Hub is misaligned before the screenshot or if the "muddy" look is a rendering artifact.

2. Coordinate Math Logging (The Hub Audit):

    In the hub_radial injection loop, add a verbose log:
    [MATH: HUB] Spoke ${index}: Angle=${angle}rad, X=${x}%, Y=${y}%.

    Log the radius_percent value being used to ensure it isn't causing spokes to "overflow" the 1200px container.

3. Theme Merge Snapshot:

    Log the final, fully merged theme object right before injection: [DEBUG: THEME_FINAL] ${JSON.stringify(activeTheme)}.

    We need to verify if the "Teal" is being replaced by a default "Blue" during the batch run.

4. SiliconFlow Payload Dump:

    In generateImage, log the exact final prompt sent to the API: [DEBUG: SF_PROMPT] ${fullPrompt}.

    Audit: Check if the agent is still prepending "8k, professional" or other "poison" keywords that clash with the "Mindfulness" vibe.

5. Playwright "Retina" Check:

    Log the Playwright viewport and scale factor used for the specific failed test: [DEBUG: RENDER] Viewport=${width}x${height}, Scale=${scaleFactor}.

6. The "Autopsy" Test Run:

    Run the "Autonomic Nervous System" Hub visualization.

    Requirement: The agent must provide:

        The terminal log containing the [MATH: HUB] and [DEBUG: SF_PROMPT] tags.

        The URL to the debug_viz_id.html file so we can inspect the raw DOM.

How this will be validated:

    I will compare the raw HTML layout to the final PNG. If the HTML looks clean but the PNG looks muddy, the issue is Playwright/Compression. If the HTML is broken, the issue is JSDOM/Math.