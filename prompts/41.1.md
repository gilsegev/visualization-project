Goal: Refactor HtmlInfographicStrategy to remove hardcoded design constants and allow for dynamic, course-specific themes passed from the orchestrator.

1. Create the Configuration File:

    Create a new file: src/image-gen/themes.config.ts.

    Move the THEME_LIBRARY constant and the ThemeId type from the strategy into this file.

    Export them so they can be used across the application.

2. Refactor HtmlInfographicStrategy:

    Import: Remove the hardcoded THEME_LIBRARY and import it from the new config file.

    Dynamic Payload Support: Update performGeneration to look for an optional custom_theme object in the task payload.

    Precedence Logic:

        If payload.custom_theme exists (provided by the Course Orchestrator), use those values.

        If not, look up the theme in THEME_LIBRARY using the blueprint.theme_id.

        Fall back to corp_blue if both fail.

3. Standardize the Image Pipeline:

    Update the generateImage method to strictly use the active theme's primary_accent and background_main for Color Forcing.

    Prompt Refinement:

        Append a strict negative prompt: --no text, blurry, distorted, messy, low quality, characters, letters.

        Ensure the image_style_suffix is pulled dynamically from the active theme.

4. JSDOM Injection Cleanup:

    Ensure the :root CSS variables injected into the HTML are derived from the same active theme object used for image generation. This guarantees that the "Teal" in the image exactly matches the "Teal" of the CSS borders.

5. Regression Test:

    Re-run scripts/test-course-batch.ts.

    Validation:

        The system must still correctly generate the "Mindfulness" course using the teal/cream palette.

        Check the logs to ensure HtmlInfographicStrategy is receiving the theme via the payload rather than reading a hardcoded "wellness" entry.

How this will be validated:

    The HtmlInfographicStrategy.ts file should be significantly shorter and contain zero hex codes or font URLs.