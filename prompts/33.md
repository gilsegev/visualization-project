Goal: Add the "Bento Grid" template and implement a "Smart Count" logic to handle 3 to 9 items dynamically.

1. Template Integration:

    Save the provided HTML as public/assets/infographics/templates/bento_grid.html.

    Update HtmlInfographicStrategy.ts to recognize bento_grid.

2. Selection Logic (The "Art Director"):

    Update generateBlueprint. Gemini should now choose bento_grid when the prompt implies a feature list, a set of equal components, or a product showcase (e.g., "The 4 pillars of health," "Features of the new iPhone").

    Constraint: If the count is 3 or 6, use the 3-column layout. If the count is 4 or 8, Gemini should suggest a 2-column or 4-column balanced look in the description notes.

3. Image-Style Synchronization:

    For the Bento Grid, update the visual_style_directive to prioritize "Flat 3D Isometric" or "Minimalist Vector" to keep the tiles clean.

    Ensure the background_style_prompt is extremely subtle (white/light grey textures) so the white cards pop.

4. The "Bento" E2E Test:

    Create scripts/test-bento-showcase.ts.

    Topic: "The 6 core features of a modern Electric Vehicle."

    Requirement: The agent must generate 6 icons in a consistent "Tech Blueprint" style and render them in the Bento Grid.

How this will be validated:

    Visually inspect bento-showcase.png.

    Verification: Does it look like an Apple Keynote slide? Are the 6 cards balanced in a 3×2 or 2×3 grid?